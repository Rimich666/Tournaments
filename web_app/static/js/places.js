console.log('places.js');
/*
const addPlaceButton = document.querySelector('#add_place_button');
const addPlaceModal = document.querySelector('.modal-container');
const locationSelectButton = document.querySelector('#place_location_select');
const popupMap = document.querySelector('.popup-map');

const map_canvas = document.querySelector('#map-canvas');

addPlaceButton.addEventListener('click', ()=>
  addPlaceModal.classList.remove('modal-container-close'));

popupMap.classList.remove('popup-close')
const map = L.map(map_canvas).setView([51.505, -0.09], 13);
L.tileLayer(
  'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',
  {
    attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors',
  },
).addTo(map);

locationSelectButton.addEventListener('click', (evt)=>{
  evt.preventDefault();
  if (popupMap.classList.contains('popup-close')){

  }
  popupMap.classList.remove('popup-close')
  evt.stopPropagation();
})*/
